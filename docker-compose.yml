version: '3'

services:
  bot:
    build: .
    restart: 'unless-stopped'
    depends_on:
      - db
  db:
    image: mongo:latest
    container_name: db
    restart: 'unless-stopped'
    ports:
      - '27017:27017'
    volumes:
      - ~/mongo/data:/data/db

